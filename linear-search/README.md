# 线性查找 Linear Search

线性查找从数组起点开始逐项比较，直到找到目标或扫描完全部元素。它不要求数据预先排序，适用于数据量较小、仅查找一次或顺序访问成本较低的场景。

## 解决的问题

- 在无序数组中定位指定值。
- 检查列表是否包含某个元素。
- 查找满足条件的第一个项目。

## 复杂度

最好时间 O(1)，平均和最坏时间 O(n)，额外空间 O(1)。

## 演示

直接打开 `index.html`，输入数组与目标值。播放或单步时探针从左向右移动，画布会保留已比较区域并显示比较次数和最终索引。
