# 计数排序 Counting Sort

计数排序针对整数值域统计每个值出现的次数，再使用累计计数把元素稳定写入正确位置。它解决小值域数据的线性时间排序问题，不依赖元素之间的比较。

时间复杂度为 O(n+k)，空间复杂度为 O(n+k)，其中 k 是值域大小。适合成绩、年龄、像素等级等范围有限的整数；值域远大于数据量时不经济。

演示依次显示频次统计、前缀累计和稳定回写，可修改 0-20 的整数数组并播放或单步执行。
