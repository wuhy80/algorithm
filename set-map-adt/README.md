# 集合与映射 ADT Set / Map

> 演示唯一键集合、键值映射及增删查操作的语义差异

## 先抓住一句话

**集合与映射 ADT Set / Map** 定义两类抽象接口：集合维护唯一元素的成员关系，映射维护唯一键到值的关联；它们不限定底层必须使用哈希表。

学习时只盯住两件事：**当前状态表示什么**，以及**这一步为什么可以排除其他可能**。演示中的颜色、指针、队列、区间或节点变化，都是这两个问题的可视化表达。

## 为什么需要它

ADT 先规定操作语义，再选择哈希表、平衡树或其他实现。这样算法可以表达“是否存在”和“键对应什么值”，而不依赖桶、旋转等实现细节。

集合重复插入同一元素不会产生副本；映射重复写入同一键通常更新它的值。

## 心智模型

可以把集合看作只存键的映射，把映射看作一张函数表。关键是键的相等性、唯一性和缺失语义，而不是具体存储布局。

面对新题时，不要先问“该套哪个模板”，先问：

1. 输入中有什么结构可以利用，例如有序性、连续区间、树形依赖、图的边或重复子问题？
2. 当前保存的状态是否足以决定下一步？
3. 哪些候选已经可以被严格证明不可能，因而永远不用再看？

## 核心不变量

> 任意时刻每个逻辑键最多出现一次；`contains/get/delete` 使用同一相等规则，更新一个键不会改变其他键的成员或映射关系。

所谓不变量，就是算法每一步开始和结束时都必须为真的事实。调试 **集合与映射 ADT Set / Map** 时，最有效的方法不是盯着最终答案，而是在每次单步后检查这条不变量。只要某一帧不再满足它，错误通常就在上一帧的边界更新、状态转移或数据结构维护中。

## 算法步骤

1. 集合用 `add`、`contains`、`delete` 表达唯一元素操作。
2. 映射用 `put` 建立或更新键值关联，用 `get` 查询。
3. 删除时区分“键不存在”和“键存在但值为空”。
4. 根据是否需要有序遍历、范围查询和最坏复杂度选择具体实现。

演示把这些步骤保存为一系列状态快照。先单步执行，确认自己能预测下一帧，再使用连续播放。若只看动画而不预测，容易记住颜色变化，却没有真正掌握决策依据。

## 框架伪代码

下面的伪代码刻意忽略页面绘制和工程细节，只保留这类算法最值得迁移的骨架：

```text
seen = Set()
seen.add(key)
if seen.contains(key): ...
seen.delete(key)

index = Map()
index.put(key, value)       # 同一 key 再次 put 会更新 value
if index.contains(key):
    value = index.get(key)
index.delete(key)
```

把伪代码映射到本目录的 `app.js` 时，可以按“解析输入 → 初始化状态 → 生成每一步 → 更新指标 → Canvas 绘制”的顺序阅读。算法逻辑负责决定状态，绘图逻辑只负责把状态呈现出来，两者不要混在一起理解。

## 跟着演示手算

1. 打开页面后先暂停，抄下初始输入，并写出你认为最重要的状态变量。
2. 点击一次“单步”前，先根据不变量预测哪些值、节点、边或区间会改变。
3. 对照状态栏观察“演示唯一键集合、键值映射及增删查操作的语义差异”是否正在发生，并解释为什么没有选择其他候选。
4. 到达终态后只改一个边界条件，例如空输入、重复值、极端顺序、断开的图或最小规模，再重新运行。
5. 最后尝试不看动画，只根据伪代码复现同一过程；能独立写出状态变化才算真正掌握。

## 复杂度怎么分析

**结论：哈希实现平均插入 / 删除 / 查询 O(1)**

不要只背大 O。分析时分三步：先数一共有多少个状态或元素，再数每个状态被处理多少次，最后把排序、堆操作、哈希查询、递归深度或额外表格单独计入。若算法具有摊还或期望复杂度，还要说明“总成本如何分摊”或“随机性假设是什么”。

## 常见错误

- 用列表模拟集合却没有阻止重复元素，破坏集合基数语义。
- 用 `get(key)==null` 判断缺失，无法区分不存在与合法空值。
- 假定所有集合/映射都保持插入顺序或键序，而接口并未承诺。
- 只用默认样例验证，没有测试空结构、单元素、重复值、断开输入或最大边界。
- 把演示中的视觉位置当作算法状态；真正应该验证的是数据、索引、距离、计数或引用关系。

## 什么时候使用

适合：去重、成员测试、频率统计、索引、关联关系和算法中的访问标记。

不适合：需要重复元素计数时应使用多重集合或映射计数；需要位置访问时数组更直接。

## 与其他算法的联系

- 先修内容：[静态数组 Static Array](https://github.com/wuhy80/algorithm/tree/main/static-array/)
- 直接后续：[图的表示 Graph Representations](https://github.com/wuhy80/algorithm/tree/main/graph-representations/)、[频率统计 Frequency Counting](https://github.com/wuhy80/algorithm/tree/main/frequency-counting/)
- 同类比较：[哈希表](https://github.com/wuhy80/algorithm/tree/main/hash-table/)、[二叉搜索树](https://github.com/wuhy80/algorithm/tree/main/binary-search-tree/)、[位集与位图](https://github.com/wuhy80/algorithm/tree/main/bitset-bitmap/)

学习顺序建议是：先用先修算法理解基础状态，再比较同类算法在“前提、维护信息、复杂度、是否可恢复答案”上的差异，最后进入把本算法作为组件的后续主题。

## 自测问题

- 不看代码，你能否用一句话说清 **集合与映射 ADT Set / Map** 在每一步维护的状态？
- 集合与映射分别如何处理重复键？为什么 `get(key)==null` 不能可靠表示键不存在？
- 把演示输入缩小到 3 到 6 个元素，能否在纸上预测下一帧再点击“单步”？
- 当前复杂度的主导项来自哪里？换一种底层数据结构后会怎样变化？

## 文件与运行

- `index.html`：演示页面结构
- `styles.css`：响应式界面样式
- `app.js`：算法实现、步骤生成和 Canvas 绘制
- `README.md`：本学习指南

## 在线查看

- [打开演示](https://wuhy80.github.io/algorithm/set-map-adt/)
- [查看源码](https://github.com/wuhy80/algorithm/tree/main/set-map-adt/)
- [返回算法目录](https://wuhy80.github.io/algorithm/)
