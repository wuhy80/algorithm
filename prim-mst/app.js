(()=>{'use strict';const c=document.getElementById('canvas'),g=c.getContext('2d',{alpha:false}),E=id=>document.getElementById(id),nodes=[['A',.13,.48],['B',.31,.22],['C',.33,.7],['D',.51,.42],['E',.62,.18],['F',.68,.7],['G',.84,.38],['H',.88,.74]],base=[[0,1,4],[0,2,7],[1,2,2],[1,3,5],[1,4,9],[2,3,3],[2,5,8],[3,4,4],[3,5,6],[3,6,7],[4,6,3],[5,6,2],[5,7,5],[6,7,4]];let graph=base.map(e=>[...e]),steps=[],k=0,running=false,speed=1,w=1,h=1,dpr=1,last=0;function build(){const seen=Array(nodes.length).fill(false),tree=[];let total=0;seen[E('start').selectedIndex]=true;steps=[];const snap=(phase,active=null,candidates=[])=>steps.push({phase,active,candidates,seen:[...seen],tree:[...tree],total});snap(`从 ${E('start').value} 开始`);while(tree.length<nodes.length-1){const candidates=graph.filter(e=>seen[e[0]]!==seen[e[1]]);snap(`比较 ${candidates.length} 条割边`,null,candidates);if(!candidates.length)break;const edge=candidates.reduce((a,b)=>a[2]<=b[2]?a:b);snap(`选择权重 ${edge[2]} 的边`,edge,candidates);tree.push(edge);total+=edge[2];seen[edge[0]]=seen[edge[1]]=true;snap(`加入节点 ${nodes[seen[edge[0]]?edge[1]:edge[0]]?.[0]||''}`,edge)}snap(tree.length===nodes.length-1?'最小生成树完成':'图不连通');k=0;running=false;ui()}const cur=()=>steps[k];function ui(){const q=cur();E('status').textContent=q.phase;E('visited').textContent=q.seen.filter(Boolean).length;E('edges').textContent=q.tree.length;E('weight').textContent=q.total;E('play').textContent=running?'暂停':k===steps.length-1?'重播':'播放';E('note').textContent=`STEP ${k+1} / ${steps.length} · CUT EDGES ${q.candidates.length}`}function step(){if(k<steps.length-1)k++;else running=false;ui()}function resize(){const r=c.getBoundingClientRect();w=r.width;h=r.height;dpr=Math.min(devicePixelRatio||1,2);c.width=w*dpr;c.height=h*dpr}function draw(){const q=cur(),pos=nodes.map(n=>({x:n[1]*w,y:105+n[2]*(h-180)}));g.setTransform(dpr,0,0,dpr,0,0);g.fillStyle='#12191b';g.fillRect(0,0,w,h);for(const e of graph){const a=pos[e[0]],b=pos[e[1]],tree=q.tree.includes(e),candidate=q.candidates.includes(e),hot=q.active===e;g.strokeStyle=tree?'#72d2a2':hot?'#f1bf58':candidate?'rgba(113,214,224,.55)':'rgba(237,245,244,.14)';g.lineWidth=tree?4:hot?3:candidate?2:1;g.beginPath();g.moveTo(a.x,a.y);g.lineTo(b.x,b.y);g.stroke();const mx=(a.x+b.x)/2,my=(a.y+b.y)/2;g.fillStyle='#12191b';g.beginPath();g.arc(mx,my,12,0,Math.PI*2);g.fill();g.fillStyle=hot?'#f1bf58':'#aab6b7';g.font='10px ui-monospace';g.textAlign='center';g.fillText(e[2],mx,my+3)}for(let i=0;i<nodes.length;i++){const p=pos[i];g.beginPath();g.arc(p.x,p.y,23,0,Math.PI*2);g.fillStyle=q.seen[i]?'#72d2a2':'#1d292b';g.fill();g.strokeStyle=q.seen[i]?'#72d2a2':'#71d6e0';g.lineWidth=q.seen[i]?3:1.5;g.stroke();g.fillStyle=q.seen[i]?'#12191b':'#edf5f4';g.font='600 12px ui-monospace';g.fillText(nodes[i][0],p.x,p.y+4)}}function frame(t){if(running&&(last===0||t-last>600/speed)){step();last=t}draw();requestAnimationFrame(frame)}E('apply').onclick=build;E('step').onclick=()=>{running=false;if(k===steps.length-1)build();step()};E('play').onclick=()=>{if(k===steps.length-1)build();running=!running;last=0;ui()};E('random').onclick=()=>{graph=base.map(e=>[e[0],e[1],Math.floor(Math.random()*9)+1]);build()};E('speed').oninput=e=>{speed=+e.target.value;E('speed-value').textContent=`${speed.toFixed(1)}×`;e.target.style.setProperty('--progress',`${(speed-.25)/2.75*100}%`)};addEventListener('resize',resize);resize();build();E('speed').dispatchEvent(new Event('input'));requestAnimationFrame(frame)})();
