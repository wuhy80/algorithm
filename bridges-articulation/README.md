# 割点与桥 Articulation Points and Bridges

割点的删除会增加图的连通分量数，桥的删除会使图断开。DFS 树通过发现时间和 Lowlink 判断子树是否能够绕过父节点返回祖先。

时间复杂度 O(V+E)，空间复杂度 O(V)。它用于网络单点故障分析、道路脆弱性和基础设施冗余评估。

演示高亮 DFS 树边、返祖边、割点和桥，并逐步更新节点的 `disc/low`。
