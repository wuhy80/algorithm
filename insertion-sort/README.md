# 插入排序 Insertion Sort

插入排序维护一个有序前缀，每次取出下一个元素，把更大的元素向右移动，再把该元素插入正确位置。

## 特点与复杂度

- 最好时间 O(n)，平均和最坏时间 O(n²)。
- 原地、稳定，额外空间 O(1)。
- 对小数组和接近有序的数据非常有效。

## 演示

直接打开 `index.html`。黄色圆形表示暂存键值，红色表示比较与移动位置，绿色表示当前有序前缀。
