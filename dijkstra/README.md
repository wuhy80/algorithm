# Dijkstra 最短路径

Dijkstra 算法计算非负权图中一个起点到其他节点的最短距离。它反复选择当前距离最小的未访问节点，并通过该节点松弛相邻边。

## 复杂度与用途

使用邻接表和优先队列时为 O((V+E) log V)，空间 O(V+E)。常用于道路导航、网络路由和代价传播。算法不能直接处理负权边。

## 演示

选择起点和终点后播放。黄色表示当前节点或边，青色节点已经固定，绿色边组成最终最短路径，节点下方显示当前距离。
