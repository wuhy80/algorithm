# 树状数组 Fenwick Tree

树状数组用紧凑数组维护前缀聚合。`lowbit(i) = i & -i` 决定 BIT[i] 覆盖区间的长度，以及查询和更新的跳转路径。

构建可达 O(n)，单点增加和前缀和查询均为 O(log n)，空间 O(n)。它比线段树简单，适合可逆前缀统计和频率累计。

演示上方显示 BIT 节点及覆盖区间，下方显示源数组；黄色是当前访问节点，路径按 `i -= lowbit(i)` 或 `i += lowbit(i)` 跳转。
