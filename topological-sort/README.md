# 拓扑排序 Topological Sort

拓扑排序把有向无环图的节点排列为线性顺序，使每条边的起点都出现在终点之前。Kahn 算法持续处理入度为 0 的节点并删除其出边。

时间复杂度 O(V+E)，空间 O(V+E)。常用于任务调度、课程依赖、构建系统和数据处理流水线。若最终输出节点少于总节点数，则图中存在环。

演示显示节点入度、零入度队列、被删除的边和最终输出顺序，并提供含环预设进行对比。
