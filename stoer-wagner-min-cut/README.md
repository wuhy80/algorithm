# Stoer-Wagner 全局最小割

> 逐阶段执行最大邻接搜索，记录割权并缩并最后两个顶点。

## 先抓住一句话

**Stoer-Wagner 全局最小割** 属于无向带权图的割与缩点算法。它无需指定源汇，通过多轮最大邻接搜索求全局最小割。

学习时只盯住两件事：**当前状态表示什么**，以及**这一步为什么可以排除其他可能**。演示中的颜色、指针、队列、区间或节点变化，都是这两个问题的可视化表达。

## 为什么需要它

对每对顶点分别跑最大流代价很高。Stoer-Wagner 每一阶段只产生一个可靠的割候选，再缩并两个顶点，最终覆盖全局最优割所需的候选。

每个阶段的最大邻接搜索会产生一个可信的割候选：最后加入顶点 `t` 时，它到已选集合的累计边权就是本阶段割值。记录候选后把 `t` 缩并到倒数第二个顶点 `s`，既缩小问题规模，又不丢失尚未覆盖的全局最小割。

## 心智模型

在一个阶段中维护集合 `A`，每次选择与 `A` 的总连接权最大的未选顶点。最后加入的两个顶点记为 `s, t`，`t` 与其余顶点之间的割权就是本阶段候选。

面对新题时，不要先问“该套哪个模板”，先问：

1. 输入中有什么结构可以利用，例如有序性、连续区间、树形依赖、图的边或重复子问题？
2. 当前保存的状态是否足以决定下一步？
3. 哪些候选已经可以被严格证明不可能，因而永远不用再看？

## 核心不变量

> 最大邻接搜索中每个未选顶点的权值等于它到已选集合 `A` 的边权总和；阶段最后顶点 `t` 的权值是该阶段的 `s-t` 最小割值。

所谓不变量，就是算法每一步开始和结束时都必须为真的事实。调试 **Stoer-Wagner 全局最小割** 时，最有效的方法不是盯着最终答案，而是在每次单步后检查这条不变量。只要某一帧不再满足它，错误通常就在上一帧的边界更新、状态转移或数据结构维护中。

## 算法步骤

1. 在当前缩并图上开始一轮最大邻接搜索，所有连接权初始化为 0。
2. 反复加入连接权最大的未选顶点，并把它的边权累加到其余候选。
3. 记录最后加入顶点 `t` 的连接权，更新全局最小割答案。
4. 将最后两个顶点 `s, t` 缩并，合并它们到其他顶点的边权，继续下一阶段。

演示把这些步骤保存为一系列状态快照。先单步执行，确认自己能预测下一帧，再使用连续播放。若只看动画而不预测，容易记住颜色变化，却没有真正掌握决策依据。

## 框架伪代码

下面的伪代码刻意忽略页面绘制和工程细节，只保留这类算法最值得迁移的骨架：

```text
best = INF
while active_vertices > 1:
    A = empty; weight[*] = 0
    repeatedly add the unused vertex with maximum weight
    let s, t be the last two added vertices
    best = min(best, weight[t])
    contract t into s and sum parallel edge weights
return best
```

把伪代码映射到本目录的 `app.js` 时，可以按“解析输入 → 初始化状态 → 生成每一步 → 更新指标 → Canvas 绘制”的顺序阅读。算法逻辑负责决定状态，绘图逻辑只负责把状态呈现出来，两者不要混在一起理解。

## 跟着演示手算

1. 打开页面后先暂停，抄下初始输入，并写出你认为最重要的状态变量。
2. 点击一次“单步”前，先根据不变量预测哪些值、节点、边或区间会改变。
3. 对照状态栏观察“逐阶段执行最大邻接搜索，记录割权并缩并最后两个顶点。”是否正在发生，并解释为什么没有选择其他候选。
4. 到达终态后只改一个边界条件，例如空输入、重复值、极端顺序、断开的图或最小规模，再重新运行。
5. 最后尝试不看动画，只根据伪代码复现同一过程；能独立写出状态变化才算真正掌握。

## 复杂度怎么分析

**结论：O(V³)**

不要只背大 O。分析时分三步：先数一共有多少个状态或元素，再数每个状态被处理多少次，最后把排序、堆操作、哈希查询、递归深度或额外表格单独计入。若算法具有摊还或期望复杂度，还要说明“总成本如何分摊”或“随机性假设是什么”。

## 常见错误

- 把图当成有向图，或缩并平行边时没有把权重相加。
- 每阶段选择最小连接权顶点，混淆“最大邻接搜索”与最短路算法。
- 记录阶段割后直接删除 `t`，却没有把 `t` 的边权合并到 `s`。
- 只用默认样例验证，没有测试空结构、单元素、重复值、断开输入或最大边界。
- 把演示中的视觉位置当作算法状态；真正应该验证的是数据、索引、距离、计数或引用关系。

## 什么时候使用

适合：无向非负带权图中不指定源汇的全局最小割，尤其是只需要一个最小割值或划分。

不适合：有向图、指定源汇的最小割、所有点对最小割查询，或大规模稀疏图上无法承受 O(V³) 的场景。

## 与其他算法的联系

- 先修内容：[Edmonds-Karp 最大流](https://github.com/wuhy80/algorithm/tree/main/edmonds-karp/)
- 直接后续：[Gomory-Hu 全局割树](https://github.com/wuhy80/algorithm/tree/main/gomory-hu-tree/)
- 同类比较：[Edmonds-Karp 最大流](https://github.com/wuhy80/algorithm/tree/main/edmonds-karp/)、[Dinic 最大流](https://github.com/wuhy80/algorithm/tree/main/dinic/)、[Gomory-Hu 全局割树](https://github.com/wuhy80/algorithm/tree/main/gomory-hu-tree/)

学习顺序建议是：先用先修算法理解基础状态，再比较同类算法在“前提、维护信息、复杂度、是否可恢复答案”上的差异，最后进入把本算法作为组件的后续主题。

## 自测问题

- 不看代码，你能否用一句话说清 **Stoer-Wagner 全局最小割** 在每一步维护的状态？
- 为什么每阶段最后加入顶点的连接权是一个割值？缩并后为什么必须累加平行边权？
- 把演示输入缩小到 3 到 6 个元素，能否在纸上预测下一帧再点击“单步”？
- 当前复杂度的主导项来自哪里？换一种底层数据结构后会怎样变化？

## 实现判定细节

- 每个阶段执行最大邻接搜索：从任意活动顶点开始，反复加入与已选集合连接权重最大的未选顶点。
- 阶段最后加入的顶点 `t` 与倒数第二个顶点 `s` 之间的割权就是 `t` 加入前累计权重，也是本阶段候选最小割。
- 记录候选后把 `t` 缩并进 `s`，将二者到其他活动顶点的权重相加，并彻底移除 `t`。
- 算法针对无向非负权图；若需恢复割集合，缩并时还要维护每个超级点包含的原始顶点。

## 文件与运行

- `index.html`：演示页面结构
- `styles.css`：响应式界面样式
- `app.js`：算法实现、步骤生成和 Canvas 绘制
- `README.md`：本学习指南

## 在线查看

- [打开演示](https://wuhy80.github.io/algorithm/stoer-wagner-min-cut/)
- [查看源码](https://github.com/wuhy80/algorithm/tree/main/stoer-wagner-min-cut/)
- [返回算法目录](https://wuhy80.github.io/algorithm/)
