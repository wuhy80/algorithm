# 堆排序 Heap Sort

堆排序先把数组构造成最大堆，使最大值位于根节点；随后反复把堆顶交换到数组末尾，缩小堆并执行下沉恢复堆性质。

## 特点与复杂度

- 构建堆 O(n)，整体排序 O(n log n)。
- 原地排序，额外空间 O(1)。
- 最坏时间稳定为 O(n log n)，但不是稳定排序。

## 演示

直接打开 `index.html`。上方显示完全二叉树，下方显示对应数组；黄色为比较或交换节点，绿色为已经固定的有序后缀。
