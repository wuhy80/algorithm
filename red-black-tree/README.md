# 红黑树 Red-Black Tree

红黑树是自平衡二叉搜索树。节点带红/黑颜色，并满足根为黑、红节点不能有红子节点、从任一节点到叶子的黑节点数量相同等性质。

插入、查找的最坏时间均为 O(log n)，空间 O(n)。它常用于有序映射、语言运行库和操作系统调度结构。

本演示重点展示插入修复：新节点先染红，再根据父节点、叔节点和祖父节点的颜色进行重新着色或旋转。黄色描边表示当前参与修复的节点。
